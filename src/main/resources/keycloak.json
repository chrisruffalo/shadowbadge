{
  "realm": "${env.REALM}",
  "auth-server-url": "${env.AUTH_URL}",
  "ssl-required": "external",
  "resource": "${env.AUTH_RESOURCE}",
  "credentials": {
    "secret": "${env.AUTH_CRED_SECRET}"
  },
  "confidential-port": 0
}